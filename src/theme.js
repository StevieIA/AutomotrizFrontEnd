import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#009688', // Color principal (verde azulado)
    },
    secondary: {
      main: '#00796b',
    },
  },
});

export default theme;
